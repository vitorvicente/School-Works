import Bank.Bank;

public class Main {

	/*
	 * Main entry method into the Banking system, creates the Bank object, and runs
	 * the GUI.
	 */
	public static void main(String[] args) {
		Bank b = new Bank();
		b.launchGUI();
	}

}
